<h1>Résultat de la recherche du mot :
	<code>
		{{ mot }}
	</code>
</h1>
<div class="row d-flex justify-content-center">
	<div class="col-9 ">
		<div class="card-deck">
			<div class="row">
				{% for produit in produits %}
					{{ include("produit/cardtest.html.twig") }}
				{% endfor %}
			</div>
		</div>
	</div>
</div>
